rootProject.name = "kfc-k2-adapter"

pluginManagement {
    plugins {
        id("com.gradle.plugin-publish") version "1.2.1"
    }
}

dependencyResolutionManagement {
    repositories {
        mavenCentral()
    }
}
